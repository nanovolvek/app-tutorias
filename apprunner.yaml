version: 1.0
runtime: python3
build:
  commands:
    build:
      - echo "Instalando dependencias..."
      - pip install --upgrade pip
      - pip install -r backend/requirements.txt
run:
  runtime-version: 3.8
  command: uvicorn app.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
  env:
    - name: DATABASE_URL
      value: "postgresql://postgres:tutorias-db-123456789@tutorias-db.cx6xogrsenqa.us-east-1.rds.amazonaws.com:5432/postgres"
    - name: SECRET_KEY
      value: "tutorias-db-123456789"
    - name: ALLOWED_ORIGINS
      value: "https://main.d1d2p1x4drhejl.amplifyapp.com,http://localhost:5173"
